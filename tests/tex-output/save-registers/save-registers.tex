\documentclass[11pt,a4paper]{article}

\usepackage{fontspec}
\usepackage[allowdeprecated=false]{gregoriotex}
\usepackage{fullpage}
\setmainfont[
    Path = ../../../fonts/ ,
    Extension = .otf ,
    UprightFont = *-Regular ,
    UprightFeatures = { SmallCapsFont = *SC-Regular } ,
    BoldFont = *-Bold ,
    BoldFeatures = { SmallCapsFont = *SC-Bold } ,
    ItalicFont = *-Italic ,
    ItalicFeatures = { SmallCapsFont = *SC-Italic } ,
    BoldItalicFont = *-BoldItalic ,
    BoldItalicFeatures = { SmallCapsFont = *SC-BoldItalic } ,
    Ligatures = TeX
]{Alegreya}

\def\showregisters{
	\begin{tabular}{r@{:~}l@{\hskip 1cm}r@{:~}l}
		hyphenpenalty & \the\hyphenpenalty & exhyphenpenalty & \the\exhyphenpenalty\\
		doublehyphendermerits & \the\doublehyphendemerits & finalhyphendemerits & 	\the\finalhyphendemerits\\
		brokenpenalty & \the\brokenpenalty & looseness & \the\looseness\\
		tolerance & \the\tolerance & pretolerance & \the\pretolerance\\
		emergencystretch & \the\emergencystretch & widowpenalty & \the\widowpenalty\\
		clubpenalty & \the\clubpenalty & parskip & \the\parskip\\
		lineskip & 	\the\lineskip & baselineskip & \the\baselineskip\\
		lineskiplimit & \the\lineskiplimit & exhyphenchar & \the\exhyphenchar
	\end{tabular}
}

\listfiles
\begin{document}
\showregisters

\gabcsnippet{(c3) test(g) <eu>o(h) i(hjh) ou(g) e(f) a(fffe) e(dc/dff)</eu> (::)}

%having finished with the score, all the registers should show the same value as before
\showregisters
\end{document}
