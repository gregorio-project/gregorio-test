% !TEX program = LuaLaTeX+se
\documentclass[11pt,a4paper]{article}

\usepackage{fontspec}
\usepackage[allowdeprecated=false]{gregoriotex}
\usepackage{fullpage}
\setmainfont[
    Path = ../../../fonts/ ,
    Extension = .otf ,
    UprightFont = *-Regular ,
    UprightFeatures = { SmallCapsFont = *SC-Regular } ,
    BoldFont = *-Bold ,
    BoldFeatures = { SmallCapsFont = *SC-Bold } ,
    ItalicFont = *-Italic ,
    ItalicFeatures = { SmallCapsFont = *SC-Italic } ,
    BoldItalicFont = *-BoldItalic ,
    BoldItalicFeatures = { SmallCapsFont = *SC-BoldItalic } ,
    Ligatures = TeX
]{Alegreya}

\begin{document}

\grechangedim{afterinitialshift}{2.2mm}{1}
\grechangedim{beforeinitialshift}{2.2mm}{1}

\grechangestyle{initial}{%
\fontsize{43}{43}\selectfont%
}

\gresetlinecolor{gregoriocolor}
%\gresetbolshifts{disable}
\grechangedim{minimalspaceatlinebeginning}{0.5mm}{1}
\makeatletter
minimalspaceatlinebeginning: \gre@dimen@minimalspaceatlinebeginning\ (default)
\makeatother

\gabcsnippet[0]{abc(abc) (Z)
def(def)}

\grechangedim{minimalspaceatlinebeginning}{2mm}{1}
\makeatletter
minimalspaceatlinebeginning: \gre@dimen@minimalspaceatlinebeginning
\makeatother

\gabcsnippet[0]{abc(abc) (Z)
def(def)}

\grechangedim{minimalspaceatlinebeginning}{3mm}{1}
\makeatletter
minimalspaceatlinebeginning: \gre@dimen@minimalspaceatlinebeginning
\makeatother

\gabcsnippet[0]{abc(abc) (Z)
def(def)}

\grechangedim{minimalspaceatlinebeginning}{4mm}{1}
\makeatletter
minimalspaceatlinebeginning: \gre@dimen@minimalspaceatlinebeginning
\makeatother

\gabcsnippet[0]{abc(abc) (Z)
def(def)}

\grechangedim{minimalspaceatlinebeginning}{8mm}{1}
\makeatletter
minimalspaceatlinebeginning: \gre@dimen@minimalspaceatlinebeginning
\makeatother

\gabcsnippet[0]{abc(abc) (Z)
def(def)}

\pagebreak
\grechangedim{minimalspaceatlinebeginning}{8mm}{1}
\makeatletter
minimalspaceatlinebeginning: \gre@dimen@minimalspaceatlinebeginning
\makeatother

\gabcsnippet[0]{abc(abc) (Z)
def(def)}

\grechangedim{minimalspaceatlinebeginning}{16mm}{1}
\makeatletter
minimalspaceatlinebeginning: \gre@dimen@minimalspaceatlinebeginning
\makeatother

\gabcsnippet[0]{abc(abc) (Z)
def(def)}

\end{document}
