% !TEX program = LuaLaTeX+se
\documentclass[11pt,a4paper,twocolumn]{article}

\usepackage{fontspec}
\usepackage[allowdeprecated=false]{gregoriotex}
\usepackage{fullpage}
\setmainfont[
    Path = ../../../fonts/ ,
    Extension = .otf ,
    UprightFont = *-Regular ,
    UprightFeatures = { SmallCapsFont = *SC-Regular } ,
    BoldFont = *-Bold ,
    BoldFeatures = { SmallCapsFont = *SC-Bold } ,
    ItalicFont = *-Italic ,
    ItalicFeatures = { SmallCapsFont = *SC-Italic } ,
    BoldItalicFont = *-BoldItalic ,
    BoldItalicFeatures = { SmallCapsFont = *SC-BoldItalic } ,
    Ligatures = TeX
]{Alegreya}

\begin{document}

\gresetlinecolor{gregoriocolor}
\bigskip\makeatletter\noindent
minimalspaceatlinebeginning: \gre@dimen@minimalspaceatlinebeginning\ (default)\\
beforealterationspace: \gre@dimen@beforealterationspace\ (default)
\makeatother

\gabcsnippet[0]{abc(def) (Z)
abc(dxdef) (Z)
dddef(def) (Z)
dddef(dxdef)}

\grechangedim{minimalspaceatlinebeginning}{0.2cm}{scalable}
\bigskip\makeatletter\noindent
minimalspaceatlinebeginning: \gre@dimen@minimalspaceatlinebeginning\\
beforealterationspace: \gre@dimen@beforealterationspace\ (default)
\makeatother

\gabcsnippet[0]{abc(def) (Z)
abc(dxdef) (Z)
dddef(def) (Z)
dddef(dxdef)}

\newpage
\grechangedim{minimalspaceatlinebeginning}{0.4cm}{scalable}
\bigskip\makeatletter\noindent
minimalspaceatlinebeginning: \gre@dimen@minimalspaceatlinebeginning\\
beforealterationspace: \gre@dimen@beforealterationspace\ (default)
\makeatother

\gabcsnippet[0]{abc(def) (Z)
abc(dxdef) (Z)
dddef(def) (Z)
dddef(dxdef)}

\grechangedim{minimalspaceatlinebeginning}{0.4cm}{scalable}
\grechangedim{beforealterationspace}{0.2cm}{scalable}
\bigskip\makeatletter\noindent
minimalspaceatlinebeginning: \gre@dimen@minimalspaceatlinebeginning\\
beforealterationspace: \gre@dimen@beforealterationspace
\makeatother

\gabcsnippet[0]{abc(def) (Z)
abc(dxdef) (Z)
dddef(def) (Z)
dddef(dxdef)}

\newpage
\grechangedim{minimalspaceatlinebeginning}{0.4cm}{scalable}
\grechangedim{beforealterationspace}{0.4cm}{scalable}
\bigskip\makeatletter\noindent
minimalspaceatlinebeginning: \gre@dimen@minimalspaceatlinebeginning\\
beforealterationspace: \gre@dimen@beforealterationspace
\makeatother

\gabcsnippet[0]{abc(def) (Z)
abc(dxdef) (Z)
dddef(def) (Z)
dddef(dxdef)}

\grechangedim{minimalspaceatlinebeginning}{0.05cm}{scalable}
\grechangedim{beforealterationspace}{0.2cm}{scalable}
\bigskip\makeatletter\noindent
minimalspaceatlinebeginning: \gre@dimen@minimalspaceatlinebeginning\\
beforealterationspace: \gre@dimen@beforealterationspace
\makeatother

\gabcsnippet[0]{abc(def) (Z)
abc(dxdef) (Z)
dddef(def) (Z)
dddef(dxdef)}

\newpage
\grechangedim{beforealterationspace}{0.4cm}{scalable}
\bigskip\makeatletter\noindent
minimalspaceatlinebeginning: \gre@dimen@minimalspaceatlinebeginning\\
beforealterationspace: \gre@dimen@beforealterationspace
\makeatother

\gabcsnippet[0]{abc(def) (Z)
abc(dxdef) (Z)
dddef(def) (Z)
dddef(dxdef)}

\end{document}
