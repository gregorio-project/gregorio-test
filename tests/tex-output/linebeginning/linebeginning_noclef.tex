% !TEX program = LuaLaTeX+se
\documentclass[11pt,a4paper,twocolumn]{article}

\usepackage{fontspec}
\usepackage[allowdeprecated=false,debug={trace,bolshift}]{gregoriotex}
\usepackage{fullpage}
\setmainfont[
    Path = ../../../fonts/ ,
    Extension = .otf ,
    UprightFont = *-Regular ,
    UprightFeatures = { SmallCapsFont = *SC-Regular } ,
    BoldFont = *-Bold ,
    BoldFeatures = { SmallCapsFont = *SC-Bold } ,
    ItalicFont = *-Italic ,
    ItalicFeatures = { SmallCapsFont = *SC-Italic } ,
    BoldItalicFont = *-BoldItalic ,
    BoldItalicFeatures = { SmallCapsFont = *SC-BoldItalic } ,
    Ligatures = TeX
]{Alegreya}
\gresetinitiallines{0}

\listfiles
\begin{document}
\gresetclef{invisible}
\gresetlinecolor{gregoriocolor}
\bigskip\makeatletter\noindent
minimalspaceatlinebeginning: \gre@space@dimen@minimalspaceatlinebeginning\ (default)\\
beforealterationspace: \gre@space@dimen@beforealterationspace\ (default)
\makeatother

\gabcsnippet{abc(def) (Z)
abc(dxdef) (Z)
dddef(def) (Z)
dddef(dxdef)}

\grechangedim{minimalspaceatlinebeginning}{0.2cm}{scalable}
\bigskip\makeatletter\noindent
minimalspaceatlinebeginning: \gre@space@dimen@minimalspaceatlinebeginning\\
beforealterationspace: \gre@space@dimen@beforealterationspace\ (default)
\makeatother

\gabcsnippet{abc(def) (Z)
abc(dxdef) (Z)
dddef(def) (Z)
dddef(dxdef)}

\newpage
\grechangedim{minimalspaceatlinebeginning}{0.4cm}{scalable}
\bigskip\makeatletter\noindent
minimalspaceatlinebeginning: \gre@space@dimen@minimalspaceatlinebeginning\\
beforealterationspace: \gre@space@dimen@beforealterationspace\ (default)
\makeatother

\gabcsnippet{abc(def) (Z)
abc(dxdef) (Z)
dddef(def) (Z)
dddef(dxdef)}

\grechangedim{minimalspaceatlinebeginning}{0.4cm}{scalable}
\grechangedim{beforealterationspace}{0.2cm}{scalable}
\bigskip\makeatletter\noindent
minimalspaceatlinebeginning: \gre@space@dimen@minimalspaceatlinebeginning\\
beforealterationspace: \gre@space@dimen@beforealterationspace
\makeatother

\gabcsnippet{abc(def) (Z)
abc(dxdef) (Z)
dddef(def) (Z)
dddef(dxdef)}

\newpage
\grechangedim{minimalspaceatlinebeginning}{0.4cm}{scalable}
\grechangedim{beforealterationspace}{0.4cm}{scalable}
\bigskip\makeatletter\noindent
minimalspaceatlinebeginning: \gre@space@dimen@minimalspaceatlinebeginning\\
beforealterationspace: \gre@space@dimen@beforealterationspace
\makeatother

\gabcsnippet{abc(def) (Z)
abc(dxdef) (Z)
dddef(def) (Z)
dddef(dxdef)}

\grechangedim{minimalspaceatlinebeginning}{0.05cm}{scalable}
\grechangedim{beforealterationspace}{0.2cm}{scalable}
\bigskip\makeatletter\noindent
minimalspaceatlinebeginning: \gre@space@dimen@minimalspaceatlinebeginning\\
beforealterationspace: \gre@space@dimen@beforealterationspace
\makeatother

\gabcsnippet{abc(def) (Z)
abc(dxdef) (Z)
dddef(def) (Z)
dddef(dxdef)}

\newpage
\grechangedim{beforealterationspace}{0.4cm}{scalable}
\bigskip\makeatletter\noindent
minimalspaceatlinebeginning: \gre@space@dimen@minimalspaceatlinebeginning\\
beforealterationspace: \gre@space@dimen@beforealterationspace
\makeatother

\gabcsnippet{abc(def) (Z)
abc(dxdef) (Z)
dddef(def) (Z)
dddef(dxdef)}

\end{document}
